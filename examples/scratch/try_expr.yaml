# try on row1.json
namespaces:
  def: "https://epimorphics.com/library/def/"
  
resources:
  - name: Test
    properties:
      "@id" : "<http://example.com/{$row}>"
      "<def:p>" : "{id | expr('(int(x) - 3) // 6')}"
